const routes = require("./activity.route");
const controller = require("./activity.controller");
const service = require("./activity.service");
const repository = require("./activity.repository");

module.exports = {
    routes,
    controller,
    service,
    repository,
};