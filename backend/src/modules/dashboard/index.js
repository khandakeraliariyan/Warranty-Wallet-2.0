const routes = require("./dashboard.route");
const controller = require("./dashboard.controller");
const service = require("./dashboard.service");
const repository = require("./dashboard.repository");

module.exports = {
    routes,
    controller,
    service,
    repository,
};